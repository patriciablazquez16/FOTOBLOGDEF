<%= form_for(@comunidad) do |f| %>
  <% if @comunidad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comunidad.errors.count, "error") %> prohibited this comunidad from being saved:</h2>

      <ul>
      <% @comunidad.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
